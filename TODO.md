# Task: Redirect Member/Provider buttons to login page

## Steps

- [x] Step 1: Add `defaultRole` prop to `LoginView.tsx` to support pre-selected role tab
- [x] Step 2: Wire up `LoginView` in `App.tsx` (import + render + onLoginSuccess handler + defaultRole state + `onNavigateLogin` handler)
- [x] Step 3: Update `Navbar.tsx` – Member button navigates to login with 'user' pre-selected via `onNavigateLogin('user')`
- [x] Step 4: Update `Navbar.tsx` – Provider button navigates to login with 'provider' pre-selected via `onNavigateLogin('provider')`
- [x] Step 5: Update `Navbar.tsx` – Login button now navigates to login page (was incorrectly opening Apply Modal)

All changes complete!

